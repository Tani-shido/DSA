#include <bits/stdc++.h>
using namespace std;

/*
// 1d array
    int main() {
        // We have to define the datatype to store in array
        int arr[5];
        cin >> arr[1] >> arr[2] >> arr[3] >> arr[4] >> arr[5];
        // cout << arr[4];
        // we can also modify and perform operations on them
        arr[4] = 10;
        cout << arr[4];
        return 0;
        // ARRAY ELEMENTS ARE STORED IN CONSECUTIVE MEMORY ADDRESS BUT NOT SURE WHERE THE FIRST (0TH) ONE WILL BE, IT CAN BE AT ANY RANDOMIZED LOCATION.
    }
*/

/*
// 2d array
    int main(){
        // we can assign rows and columns in this array 
        int arr[2][2];
        arr[0][0] = 12;
        cout << arr[0][0];
        return 0;
    }
*/