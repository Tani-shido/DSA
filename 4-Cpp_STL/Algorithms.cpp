#include<bits/stdc++.h>
using namespace std;

void Sorting(){

    int a[] = {3,2,1,4};
    // To sort any container use sort
    sort(a, a + 4);

    vector<int>v;
    sort(v.begin(), v.end());

    // to sort in assecending order
    // sort(a, a + 4, greater<int>);


}













int main (){

    Sorting();
    return 0;
}